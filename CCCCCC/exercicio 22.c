#include <stdio.h>
#include <stdlib.h>
#include <locale.h>
#include <string.h>
int main(){
	setlocale(LC_ALL,"");
	char nome[50];
	int tamanho;
	int conv;
	printf("DIGITE SEU NOME:");
	fgets(nome, 50 ,stdin); // faz com que nome receba o nome de alguém
	tamanho = strlen(nome); //quantidade de caracteres em nome
	
	for (i=0,i<=tamanho,i++){
		conv = ()
	}
	
	printf("%i \n",tamanho);
	//printf("%c",69);
	//printf("%i",'A');
	return 0;
}
